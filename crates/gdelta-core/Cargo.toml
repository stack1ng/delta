[package]
name = "gdelta-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "GDelta structural binary delta: one-shot encode and incremental streaming decode"

[lib]
name = "gdelta_core"
